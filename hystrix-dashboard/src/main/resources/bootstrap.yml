spring:
  application:
    name: hystrix-dashboard
  cloud:
    config:
      discovery:
        enabled: false

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

